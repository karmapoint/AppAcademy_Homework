<h1> All the bands </h1>


<% @bands.each do |band| %>
  <ul>
    <li> <strong><%= link_to band.name, band_url(band)  %></strong> | <%= link_to "Edit band", edit_band_url(band) %> | <%= link_to "Delete", band, method: :delete %></li>
  </ul>
<% end  %>

<br>
<%= link_to "Add a band", new_band_url %>
